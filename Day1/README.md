# 📄 Reading Log File Lines

This project reads a sample log file (`sample.log`), prints its content line by line (if verbose), and returns the total number of lines.

## 🛠 How to Run

```bash
python main.py
```
